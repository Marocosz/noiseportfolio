# --- CONFIGURAÇÕES PRINCIPAIS DE IA ---
# Define qual fornecedor de IA será usado para gerar respostas.
# Opções: openai | groq | gemini
LLM_PROVIDER=openai

# --- API KEYS (CREDENCIAIS) ---
# Necessária se LLM_PROVIDER=groq
GROQ_API_KEY=

# NECESSÁRIA PARA EMBEDDINGS (RAG)
# O sistema usa modelos do Google (Gemini) para gerar vetores de memória.
# Obrigatória independente do LLM provider escolhido acima.
GOOGLE_API_KEY=

# Necessária se LLM_PROVIDER=openai
OPENAI_API_KEY=
# --- CONFIGURAÇÕES DE DEPLOY ---
# Define se o banco de dados deve ser recriado ao iniciar o container.
# Útil quando voce altera os arquivos .md em 'data/' e quer atualizar a memória.
# Default: false
FORCE_REINGEST=false
